# Метка 1 — алерты (устарела, до cross-TF)

**Старая метка.** Актуальный эталон — [**Метка 2**](MARKER_2.md).

**Зафиксировано:** 2026-05-25  
**Коммит:** `cd152b6` — 1m при просмотре 1m (JWT, Telegram, звук MP3). База: `a8d79cf`.

## Что работало

- Алерт на **1m** при просмотре **1m**: звук, тост, линия, Telegram.
- Несколько алертов подряд и одновременно.
- Push/trigger через worker + `ct_supabase_auth`.

## Известное ограничение (исправлено в метке 2)

- Алерт на **1m**, график на **1h**: только Telegram, без звука в браузере, линия могла остаться.

## Откат к метке 1

```bash
git checkout cd152b6 -- js/ alert-worker/lib/ sounds/ docs/
```
