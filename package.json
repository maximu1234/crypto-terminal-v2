{
  "private": true,
  "description": "Static site + Vercel serverless /api (CommonJS handlers)",
  "scripts": {
    "assets:sync": "node scripts/sync-asset-versions.cjs",
    "assets:bump": "node scripts/sync-asset-versions.cjs bump",
    "assets:check": "node scripts/check-asset-manifest.cjs",
    "nav:check": "node scripts/check-site-nav.cjs",
    "bundle:sync": "npm run bundle:site --prefix desktop",
    "bundle:check": "node scripts/check-desktop-bundle.cjs",
    "test": "node --test tests/*.test.mjs",
    "check:all": "node scripts/pre-refactor-check.cjs"
  }
}
